# 工单事务边界（简化版）

- `ticket_info` 与 `ticket_log` **同事务**（事件流原子；缺一日志等于业务错误）
- **AI 调用事务外**：RestTemplate 调 DeepSeek HTTP，try-catch 捕获异常，失败返回模板回复，**不影响工单创建**
- 不做 retry queue / dead letter / 事务后事件（项目规模用不到）

## 面试怎么说

"工单信息与状态变更日志走同事务保证原子性；AI 调用是外部依赖，我把它放到事务外，失败时记录错误日志并返回模板回复，不阻塞主流程"。