# Java / Spring Boot
target/
build/
out/
*.class
*.jar
*.war
*.log
hs_err_pid*

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr
.project
.classpath
.settings/
# Eclipse / VS Code Java：编译输出目录与注解处理器配置（Lombok 会生成 .factorypath）
bin/
.factorypath
.apt_generated/
.apt_generated_tests/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets
.env
.env.local
application-local.yml
application-local.properties

# Scratch (per AGENTS.md: issues live under .scratch/)
.scratch/

# 设计稿：本地参考资料，不入库、不随代码分发（ticket 15 的 .dockerignore 亦排除 mockups/）
mockups/

# 项目内部协作文档：仅本地查阅，不入 GitHub 公开仓库
# （CONTEXT.md 是项目上下文，docs/adr + docs/agents 是 ADR 与 agent 协作约定）
CONTEXT.md
docs/

# Node (if any tooling added later)
node_modules/
dist/