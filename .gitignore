# Java / Spring Boot
target/
build/
out/
*.class
*.jar
*.war
*.log
hs_err_pid*

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr
.project
.classpath
.settings/
# Eclipse / VS Code Java：编译输出目录与注解处理器配置（Lombok 会生成 .factorypath）
bin/
.factorypath
.apt_generated/
.apt_generated_tests/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets
.env
.env.local
application-local.yml
application-local.properties

# Scratch (per AGENTS.md: issues live under .scratch/)
.scratch/

# 设计稿：本地参考资料，不入库、不随代码分发（ticket 15 的 .dockerignore 亦排除 mockups/）
mockups/

# 教学 workspace：本地学习记录 / 速查表 / lesson，私人学习用，不入库
teach/

# README.md 入版本控制（项目主文档）
# CONTEXT.md 是项目内部词汇表，不入库
# docs/agents/ 是 AI 协作约定（个人 AI 工具链），不入库
# docs/ 下其他目录（adr / issues / plans）按"找实习展示用"策略选择性提交
CONTEXT.md
docs/agents/

# Node (if any tooling added later)
node_modules/
dist/

# 本地附件降级存储（ticket 12）：aliyun.oss.enabled=false 时文件落 ./tmp/oss/，不入库
tmp/